Follow the repository setup contract in `AGENTS.md` and the workflow in `docs/setup-guide/agent-setup.md`. Never request secrets in chat, never print `.env`, keep production data outside the repository, and do not push changes without explicit user approval.

